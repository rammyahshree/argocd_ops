apiVersion: v1
kind: Service
metadata:
  name: {{ include "testbook.fullname" . }}
  labels:
    {{- include "testbook.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "testbook.selectorLabels" . | nindent 4 }}
