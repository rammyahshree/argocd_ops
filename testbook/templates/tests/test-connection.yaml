apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "testbook.fullname" . }}-test-connection"
  labels:
    {{- include "testbook.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "testbook.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
